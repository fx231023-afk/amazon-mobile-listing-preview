services:
  - type: web
    name: amazon-mobile-listing-preview
    env: node
    plan: starter
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MAX_BODY_MB
        value: 80
